[plugin]
id       = "archive"
version  = "1.0.0"
name     = "Archive Support"
enabled  = true

[plugin.provides]
services = ["archive.read", "archive.write", "archive.capabilities"]

[plugin.requires]
services = ["fs.read", "fs.write", "fs.resolve"]
plugins  = []
